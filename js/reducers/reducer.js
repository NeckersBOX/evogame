const initialState = {
  generation: 0,
  solutions: 0,
  day: 0
};

const reducer = (state = initialState, action) => {
  let nextState = state;

  switch(action) {
    default:
      break;
  }

  return nextState;
}

export default reducer;
