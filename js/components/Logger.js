import { h, Component } from 'preact'

class Logger extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div id="evogame-info">
        Not initialized...
      </div>
    );
  }
}

export default Logger;
