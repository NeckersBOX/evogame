import { h, Component } from 'preact'

class WorldMap extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div>
        Not initialized...
      </div>
    );
  }
}

export default WorldMap;
