import { h, Component } from 'preact'

class Parameters extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div>
        <h1>Parameters</h1>
      </div>
    );
  }
}

export default Parameters;
